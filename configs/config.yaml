defaults:
  - model: default
  - data: default
  - train: phase1
  - _self_

hydra:
  run:
    dir: logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: logs/multirun/${now:%Y-%m-%d}
    subdir: ${hydra:job.num}